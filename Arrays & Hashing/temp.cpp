#include<bits/stdc++.h>
using namespace std;

int main(){
    // class Substring
    // string s = "abc:def";
    // cout << s.find(":") << endl;
    // cout << s.substr(0, s.find(":")) << endl;  
    // cout << s.substr(s.find(":") + 1) << endl;


    
    return 0;
}